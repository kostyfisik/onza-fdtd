# Comment start with sign '#' and finish at the end of the same line.
# All spaces are ignored.
# Values are maped in format 
#   key_name = key_value
# Order is not saved
################################################################################
# Phoif allows usage of up to 16 nodes with 128 processes. Creating config dependence with minimum 128 length_x and 400 total_time_steps

test_case = TMz2Dspeedup
length_x = 3550
length_y = 3550
total_time_steps = 100
