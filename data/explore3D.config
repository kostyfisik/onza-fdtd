# Comment start with sign '#' and finish at the end of the same line.
# All spaces are ignored.
# Values are maped in format 
#   key_name = key_value
# Order is not saved
################################################################################
# Phoif allows usage of up to 16 nodes with 128 processes. Creating config dependence with minimum 185 length_x and 100 total_time_steps

test_case = 3Dsimple
length_x = 185
length_y = 185
length_z = 185
total_time_steps = 100
